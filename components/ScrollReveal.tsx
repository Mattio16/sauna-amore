// Deprecated — replaced by components/site/Reveal.tsx (phase 3 redesign).
import { ReactNode } from 'react';
export default function ScrollReveal({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
